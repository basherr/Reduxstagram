import React from 'react';

const PhotoGrid = React.createClass({
    render() {
        return (
            <div>
                Photo Grid section
            </div>
        )
    }
});

export default PhotoGrid;